#include<stdio.h>
#include<conio.h>
main()
{
	int w, l, a;
	printf("Length of rectangle");
	scanf("%d",&l);
	printf("Width of rectangle");
	scanf("%d",&w);
	a=l*w;
	printf("Area of Rectangle is %d",a);
}